# Operator golden-path merge proof

Throwaway PR opened by flock-leader (external agent, read-only on GitHub) to prove the
operator approve-checks + node-scoped merge golden path end to end:
held fork-PR checks -> operator run-checks -> green -> operator merge -> zero human GitHub action.

Safe to delete after the proof.
